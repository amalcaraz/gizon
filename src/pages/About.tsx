import React from 'react';
import styled from 'styled-components'

const AboutContainer = styled.div`
  background-color: rgba(10, 10, 10, 0);
  height: 102px;
`

export const About: React.FC = () => {
  return (<AboutContainer>content</AboutContainer>)
}