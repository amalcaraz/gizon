import React from 'react';
import styled from 'styled-components'

const HomeContainer = styled.div`
  background-color: rgba(10, 10, 10, 0);
  height: 102px;
`

export const Home: React.FC = () => {
  return (<HomeContainer>content</HomeContainer>)
}